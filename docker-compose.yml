version: "3.9"

services:
  gorzdrav_parser:
    build: .
    container_name: gorzdrav_parser
    ports:
      - "8003:8000"
    restart: always
    networks:
      - parser-network

networks:
  parser-network:
    external: true
